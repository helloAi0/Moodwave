# MoodWave Desktop Web — Python dependencies
# Install with:  pip install -r requirements.txt

flask==3.0.3
flask-socketio==5.3.6
eventlet==0.36.1          # async driver for flask-socketio

opencv-python==4.9.0.80
deepface==0.0.92
tensorflow-cpu==2.16.1    # lightweight CPU-only inference

pygame==2.5.2             # for music playback with crossfade

# deepface auto-installs most of its deps, but these help on some systems:
numpy>=1.24,<2.0
pillow>=10.0