/** @type {import('next').NextConfig} */
const nextConfig = {
  // This allows Next.js to run safely on your local network
  allowedDevOrigins: ['192.168.0.167', 'localhost'],
}

module.exports = nextConfig